<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context=".activities.SharePhotoActivity">

        <Button
            android:id="@+id/button_share_photo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:background="@color/button_login_fb"
            android:padding="@dimen/padding_common"
            android:text="Share Photo"
            android:textColor="@android:color/white"/>

        <ImageView
            android:id="@+id/image_photo"
            android:layout_width="match_parent"
            android:layout_height="500dp"
            android:layout_below="@id/button_share_photo"
            android:layout_centerHorizontal="true"
            android:paddingTop="@dimen/padding_common"/>

        <TextView
            android:id="@+id/text_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/image_photo"
            android:paddingTop="@dimen/padding_common"
            android:text="@string/text_caption"
            android:visibility="gone"/>

        <EditText
            android:id="@+id/et_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/text_caption"
            android:hint="@string/text_caption"
            android:paddingTop="@dimen/padding_common"
            android:visibility="gone"/>

        <Button
            android:id="@+id/button_share"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_below="@id/et_caption"
            android:layout_centerHorizontal="true"
            android:background="@color/colorPrimary"
            android:padding="@dimen/padding_common"
            android:text="@string/button_share"
            android:textColor="@android:color/white"
            android:visibility="gone"/>

    </RelativeLayout>
</ScrollView>
